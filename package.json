{
  "name": "git-commit-helper-0226",
  "publisher": "Danny0423",
  "displayName": "Git Commit Helper 0226",
  "description": "快速產生符合團隊規範的 git commit message。使用方式：按 Ctrl+Alt+C（Mac: Cmd+Alt+C）或開啟 Command Palette 搜尋 Git: Smart Commit，依序選擇 commit 類型、輸入 scope（選填）、描述訊息、issue 序號（選填），確認後自動執行 git commit。",
  "version": "0.0.3",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "git-commit-helper.commit",
        "title": "Git: Smart Commit",
        "icon": "$(git-commit)"
      }
    ],
    "menus": {
      "scm/title": [
        {
          "command": "git-commit-helper.commit",
          "group": "navigation",
          "when": "scmProvider == git"
        }
      ]
    },
    "keybindings": [
      {
        "command": "git-commit-helper.commit",
        "key": "ctrl+alt+c",
        "mac": "cmd+alt+c"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package:patch": "npm version patch && vsce package --allow-missing-repository",
    "package:minor": "npm version minor && vsce package --allow-missing-repository",
    "package": "vsce package --allow-missing-repository"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.3.0"
  }
}
